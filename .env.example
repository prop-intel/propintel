# Shared environment variables for all apps
# Copy this file to .env and fill in the values

# Database (used by both web and api apps)
DATABASE_URL="postgresql://postgres:password@localhost:5432/propintel"

# See apps/web/.env.example for web-specific variables (auth, etc.)
# See apps/api/.env.example for api-specific variables (OpenAI, etc.)
