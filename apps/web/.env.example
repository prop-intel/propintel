# Database
DATABASE_URL="postgresql://postgres:password@localhost:5432/propintel"

# NextAuth
# Generate with: npx auth secret
AUTH_SECRET="dev_secret_placeholder"

# Google OAuth
# Get from https://console.cloud.google.com/apis/credentials
# NOTE: Required for build. Use "placeholder" values for local dev if not using auth.
AUTH_GOOGLE_ID="placeholder_id"
AUTH_GOOGLE_SECRET="placeholder_secret"

# App URL (used for tracking scripts and callbacks)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# API Backend Connection (server-side only - for calling the separate API)
# API_URL="http://localhost:4000"
# API_KEY="propintel-dev-key-2024"

# S3 Storage (for direct report reads - server-side only)
# AWS_REGION=us-west-2
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# S3_BUCKET=propintel-api-dev-storage

# Local Development Storage
# USE_LOCAL_STORAGE=true
# LOCAL_STORAGE_PATH=../api/.local-storage
